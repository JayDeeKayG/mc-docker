FROM alpine:latest

RUN apk update && apk add --no-cache bash wget findutils gawk

RUN apk add --no-cache openjdk25

RUN mkdir /scripts
RUN mkdir /config
RUN mkdir /minecraft

COPY Daemon.sh /scripts/Daemon.sh

WORKDIR /minecraft

EXPOSE 25565

ENTRYPOINT ["/bin/bash", "/scripts/Daemon.sh"]
